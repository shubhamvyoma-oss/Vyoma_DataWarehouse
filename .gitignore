# Environment — contains database credentials, never commit this
.env

# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/

# Logs — generated at runtime, not source files
ingestion/webhook_receiver.log
ingestion/webhook_receiver.log.*
ingestion/server_startup.log

# Virtual environments
venv/
.venv/
env/
